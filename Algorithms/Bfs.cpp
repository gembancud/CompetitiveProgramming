#include <bits/stdc++.h>
using namespace std;
int movr[]={-1,1,0,0};
int movc[]={0,0,-1,1};

int main(){
    vector<vector<int>> g(10,vector<int>(10));
    vector<vector<bool>> bg(10,vector<bool>(10,false));
    queue<int> q;
    q.push(0);
    while(!q.empty){
        int tmp= q.front();q.pop();
        for(int i=0;i<4;i++){
        }
    }
}